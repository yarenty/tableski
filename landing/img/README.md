Images for the landing page. `hero.jpg` is the picture next to the headline (1024×1024, JPEG,
under ~400 KB); the figure hides itself while the file is missing. Published together with
`index.html` by the site's publish step.
